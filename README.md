Same as [github.com/xplshn/linox](https://github.com/xplshn/linox) but with Curl

> We track the Linux kernel's stable releases. We apply to them the latest Oasis Linux patches. We tag a release everytime that the patches apply succesfully to the new kernel release and we confirm the kernel builds. The branch you're interested in is linox.

Here, instead, we track Curl (fixed at 8.17.0 for now) and apply patches for it to work with bearSSL
